import { DefaultTheme } from 'styled-components/dist/types'

export const lightThmem: DefaultTheme = {
	textColor: 'black',
	bgColor: 'white',
	btnColor: 'tomato,'
};

export const darkThmem: DefaultTheme = {
	textColor: 'white',
	bgColor: 'black',
	btnColor: 'teal',
};